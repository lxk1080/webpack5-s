// 测试自定义的 style-loader 和自定义的 file-loader
import './css/index.css'

const a = 10
const b = 20
const c = a + b

// 测试自定义的 clean-log-loader
console.log('this is main file')

// 测试自定义的 babel-loader
const func = (...args) => args.join('')
